# Runtime credentials. Never commit a populated .env file.
EODHD_API_TOKEN=
OPENAI_API_KEY=
# Models are configured in config/settings.yaml. The cost-controlled defaults
# are gpt-5.4-mini for first pass and gpt-5.4 for selective escalation.

# Local paths and logging.
DATA_ROOT=./data
DUCKDB_PATH=./data/research.duckdb
LOG_LEVEL=INFO
